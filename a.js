const numbers = [0]
var maxnumber = numbers.length
console.log (`o valor inicial é ${maxnumber}`)
if (maxnumber < 4) {
    while (maxnumber < 4) {
        maxnumber++
        console.log (maxnumber)
    }
} else {
    console.log (`${numbers} é o maximo de numeros`)
}

